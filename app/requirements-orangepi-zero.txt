# ❌ НЕ встановлюємо uvicorn (потребує C-компіляції)
# ✅ Встановлюємо FastAPI + асинхронний сервер окремо

fastapi==0.104.1        # старша = більше wheels
python-multipart==0.0.6 # для форм
pydantic==2.4.2         # більше wheels на ARM

# 🔴 КРИТИЧНО: paho-mqtt БЕЗ C-розширень!
paho-mqtt==1.6.1        # pure-Python версія, ДУЖЕ важлива версія!

# ✅ Вже є в базовому Python
# pyserial вже встановлений як pure-Python

# numpy через apt, не pip (заощадимо час)
# ВСТАНОВИМО ОКРЕМО нижче

# tflite_runtime - скопіюємо pre-built wheel!
# (гляди крок 4)

# Лайте для uvicorn (з C-расширениями = вік)
# Замість використаємо simple WSGI-сервер:
waitress==2.1.2         # чистий Python, швидко на ARM
